% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_hmnp_data}
\alias{simulate_hmnp_data}
\title{Simulate hierarchical multinomial probit data}
\usage{
simulate_hmnp_data(
  N = 500,
  T = 10,
  J = 4,
  beta = c(0.8, -0.6),
  W = NULL,
  theta = c(0.5, -0.4),
  sigma_d = 0.5,
  Z = NULL,
  include_outside = TRUE,
  seed = 123,
  vary_choice_set = FALSE,
  sigma = 1
)
}
\arguments{
\item{N}{Number of respondents.}

\item{T}{Number of choice situations per respondent.}

\item{J}{Number of inside alternatives.}

\item{beta}{Population means of the structural random coefficients
(length \code{K_x = length(beta)}); all coordinates are normal.}

\item{W}{Covariance of the random coefficients (\code{K_x} x \code{K_x}).
Defaults to \code{diag(0.5, K_x)}.}

\item{theta}{Mean-function coefficients for
\eqn{\delta_j = z_j'\theta + \xi_j}; the first entry is the intercept,
entries \verb{2..P} load on the alternative-level covariates.}

\item{sigma_d}{Standard deviation of the alternative-level effects
\eqn{\xi_j}.}

\item{Z}{Optional \verb{J x (length(theta) - 1)} matrix of alternative-level
covariates (excluding the intercept). Default \code{NULL} draws them
Uniform(-1, 1).}

\item{include_outside}{Logical; if \code{TRUE} (default) every choice set also
contains an outside option with systematic utility 0 and its own normal
shock. The outside good is \emph{implicit} in the returned data (matching the
\code{\link[=prepare_hmnp_data]{prepare_hmnp_data()}} convention): no physical row is emitted, and a
choice situation the outside option wins has an all-zeros \code{choice}
column.}

\item{seed}{Random seed (\code{NULL} skips \code{set.seed()}).}

\item{vary_choice_set}{Logical; if \code{TRUE} choice-set size is sampled
uniformly from \code{2:J} per task. Default \code{FALSE}.}

\item{sigma}{Standard deviation of the iid utility shocks (DGP scale).}
}
\value{
A \code{choicer_sim} object. \code{true_params} contains \code{beta}, \code{W},
\code{theta}, \code{sigma_d}, the realized \code{delta} and \code{xi}, and the full
mean-function design \code{Z} — all on the identified scale (see Details).
}
\description{
Generates synthetic panel choice data from the hierarchical probit DGP
with iid normal utility shocks:
\deqn{U_{ijt} = x_{ijt}'\beta_i + \delta_j + \epsilon_{ijt}, \qquad
      U_{iot} = \epsilon_{iot}, \qquad
      \epsilon \sim N(0, \sigma^2),}
choice by argmax within the task. The outside option is stochastic — it
carries its own \eqn{N(0, \sigma^2)} shock on top of systematic utility
0, exactly as in the estimator. \eqn{\beta_i \sim N(\beta, W)} (normal
only) and \eqn{\delta_j = z_j'\theta + \xi_j},
\eqn{\xi_j \sim N(0, \sigma_d^2)}, as in \code{\link[=simulate_hmnl_data]{simulate_hmnl_data()}}.
}
\details{
The iid-probit likelihood identifies parameters only up to the common
scale \eqn{\sigma}, so \code{true_params} is reported on the \emph{identified}
scale: \code{beta} \eqn{= \beta/\sigma}, \code{W} \eqn{= W/\sigma^2}, \code{theta}
\eqn{= \theta/\sigma}, \code{sigma_d} \eqn{= \sigma_d/\sigma}, \code{delta}
\eqn{= \delta/\sigma}, \code{xi} \eqn{= \xi/\sigma}. With the default
\code{sigma = 1} the DGP scale and the identified scale coincide.
}
\examples{
\donttest{
sim <- simulate_hmnp_data(N = 100, T = 4, J = 4, seed = 123)
print(sim)
sim$true_params$delta
}
}
