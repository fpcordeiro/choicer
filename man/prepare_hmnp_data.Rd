% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmnprobit_utils.R
\name{prepare_hmnp_data}
\alias{prepare_hmnp_data}
\title{Prepare inputs for hierarchical multinomial probit estimation}
\usage{
prepare_hmnp_data(
  data,
  id_col,
  alt_col,
  choice_col,
  covariate_cols,
  person_col = NULL,
  alt_covariate_cols = NULL,
  outside_opt_label = NULL,
  cf_residual_col = NULL,
  include_outside_option = TRUE
)
}
\arguments{
\item{data}{Data frame containing choice data.}

\item{id_col}{Name of the column identifying choice situations (tasks).
Task ids only need to be unique within a respondent.}

\item{alt_col}{Name of the column identifying alternatives.}

\item{choice_col}{Name of the column indicating the chosen alternative
(1 = chosen, 0 = not chosen).}

\item{covariate_cols}{Vector of names of structural covariate columns
(the random-coefficient dimensions).}

\item{person_col}{Name of the respondent column grouping choice
situations. \code{NULL} (default) makes each choice situation its own
respondent.}

\item{alt_covariate_cols}{Names of alternative-level covariate columns
(constant within each alternative) forming the \eqn{\delta} mean
function. \code{NULL} (default) gives an intercept-only design (P = 1).}

\item{outside_opt_label}{Label of physical outside-option rows, removed
when \code{include_outside_option = TRUE} (the outside good is implicit).}

\item{cf_residual_col}{Name of a first-stage residual column (control
function for an endogenous covariate), appended to \code{X}. Default \code{NULL}.}

\item{include_outside_option}{Logical; if \code{TRUE} (default) an implicit
outside option with systematic utility 0 is part of every choice set.}
}
\value{
A list of class \code{c("choicer_data_hmnp", "list")} with the same
components as \code{\link[=prepare_hmnl_data]{prepare_hmnl_data()}} (minus \code{rc_dist}).
}
\description{
Prepares and validates panel (or cross-sectional) choice data for the
hierarchical Bayesian multinomial probit with iid \eqn{N(0, \sigma^2)}
utility shocks. The model shares its two-level random-effect structure
with \code{\link[=prepare_hmnl_data]{prepare_hmnl_data()}}: respondent-level structural tastes
\eqn{\beta_i \sim N(b, W)} over the \code{covariate_cols} (normal only — the
probit keeps full conjugacy), and a global alternative-level effect
\eqn{\delta_j = z_j'\theta + \xi_j}, \eqn{\xi_j \sim N(0, \sigma_d^2)}.
}
\details{
The returned structure is identical to \code{\link[=prepare_hmnl_data]{prepare_hmnl_data()}} (both preps
share one internal engine), except there is no \code{rc_dist} field. Unlike
\code{\link[=prepare_mnp_data]{prepare_mnp_data()}}, utilities are NOT differenced against a base
alternative: the iid-shock model works in un-differenced utility space,
so unbalanced choice sets are supported and the outside option is
implicit (its latent utility is a stochastic \eqn{N(0, \sigma^2)} draw in
the kernel, systematic utility 0).
}
\examples{
library(data.table)
set.seed(42)
N <- 20; T <- 3; J <- 4
dt <- data.table(
  pid  = rep(1:N, each = T * J),
  task = rep(seq_len(N * T), each = J),
  alt  = rep(1:J, N * T)
)
dt[, `:=`(x1 = rnorm(.N), x2 = runif(.N, -1, 1))]
dt[, choice := 0L]
dt[, choice := if (runif(1) < 0.8) sample(c(1L, rep(0L, J - 1))) else 0L,
   by = task]
input <- prepare_hmnp_data(dt, "task", "alt", "choice", c("x1", "x2"),
                           person_col = "pid")
input$Ti[1:5]
input$alt_mapping
}
\seealso{
\code{\link[=prepare_hmnl_data]{prepare_hmnl_data()}} for the component-by-component description.
}
