% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_mxl_data}
\alias{simulate_mxl_data}
\title{Simulate mixed logit data}
\usage{
simulate_mxl_data(
  N = 5000,
  J = 4,
  beta = c(0.8, -0.6),
  delta = NULL,
  mu = NULL,
  Sigma = matrix(c(1, 0.5, 0.5, 1.5), nrow = 2),
  rc_dist = NULL,
  rc_correlation = NULL,
  price_cols = NULL,
  seed = 123,
  outside_option = TRUE,
  vary_choice_set = TRUE
)
}
\arguments{
\item{N}{Number of choice situations.}

\item{J}{Number of inside alternatives.}

\item{beta}{Fixed coefficients for \verb{x1..x\{K_x\}} (length \code{K_x = length(beta)}).}

\item{delta}{ASCs for inside alternatives (length \code{J}). Defaults to an
alternating pattern of \code{c(0.5, -0.5)}.}

\item{mu}{Mean shifter for random coefficients (length \code{K_w = ncol(Sigma)}).
Defaults to a zero vector.}

\item{Sigma}{Covariance matrix of random coefficients (square, \verb{K_w x K_w}).}

\item{rc_dist}{Integer vector (length \code{K_w}): \code{0L} for normal, \code{1L} for
log-normal. Default \code{NULL} is treated as all-normal.}

\item{rc_correlation}{Logical; if \code{NULL} (default) it is auto-detected from
the off-diagonal entries of \code{Sigma}.}

\item{price_cols}{Character vector of \verb{w*} column names to draw as
\code{-Uniform(0.1, 3)} instead of \code{Uniform(-1, 1)}. Default \code{NULL}.}

\item{seed}{Random seed (\code{NULL} skips \code{set.seed()}).}

\item{outside_option}{Logical; include outside option with \code{alt = 0}.}

\item{vary_choice_set}{Logical; if \code{TRUE} (default) choice set size is
sampled uniformly from \code{2:J}.}
}
\value{
A \code{choicer_sim} object. \code{true_params} includes \code{beta}, \code{delta},
\code{Sigma}, \code{L_params} (packed Cholesky parameters), \code{mu}, \code{rc_dist},
\code{rc_correlation}.
}
\description{
Generates synthetic choice data with random coefficients drawn from a
multivariate normal (optionally log-normal per dimension) and an additional
mean shifter \code{mu}. Random coefficients are parameterized via the lower
Cholesky factor of \code{Sigma}. Covariates are Uniform(-1, 1) by default;
columns named in \code{price_cols} are drawn as \code{-Uniform(0.1, 3)} to mimic
strictly-negative price variables.
}
\details{
Random coefficients are constructed to match the estimator's
parameterization in \code{src/mxlogit.cpp}. For every dimension the raw draw
is \code{L \%*\% eta} where \code{eta ~ N(0, I)}. A normal random coefficient
(\code{rc_dist = 0}) is then \verb{gamma_k = mu_k + (L \%*\% eta)_k}. A log-normal
random coefficient (\code{rc_dist = 1}) follows the shifted log-normal
\verb{beta_k = exp(mu_k) + exp((L \%*\% eta)_k)} -- not the textbook
\code{exp(mu_k + sigma_k * eta)} -- so \code{mu_k} in \code{true_params$mu} is on the
same scale the estimator recovers and \code{recovery_table()} can compare
like-for-like.
}
\examples{
\donttest{
sim <- simulate_mxl_data(N = 1000, J = 4, seed = 123)
print(sim)
}
}
