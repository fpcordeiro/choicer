% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_nl_data}
\alias{simulate_nl_data}
\title{Simulate nested logit data}
\usage{
simulate_nl_data(
  N = 10000,
  beta = c(1.5, -0.8),
  delta = c(`1` = 0.5, `2` = 0.3, `3` = -0.2, `4` = -0.5, `5` = 0.4),
  nests = list(c(1, 2), c(3, 4, 5)),
  lambdas = c(0.8, 0.2),
  seed = 123
)
}
\arguments{
\item{N}{Number of choice situations.}

\item{beta}{Fixed coefficients for covariates \verb{X, W} (length 2 by default).}

\item{delta}{Named numeric vector of ASCs for inside alternatives.}

\item{nests}{List of integer vectors defining nest membership for inside
alternatives.}

\item{lambdas}{Numeric vector of dissimilarity parameters, one per nest.}

\item{seed}{Random seed (\code{NULL} skips \code{set.seed()}).}
}
\value{
A \code{choicer_sim} object. \code{true_params} includes \code{beta}, \code{delta},
\code{lambdas}; \code{settings} includes the \code{nest_structure}. The returned
\code{data} retains a \code{nest} column (integer, with \code{0L} for the outside
option) for convenient use with \code{\link[=run_nestlogit]{run_nestlogit()}}.
}
\description{
Generates synthetic choice data with nested logit probabilities computed
analytically (log-sum-exp over inclusive values), then samples choices from
the implied multinomial. The outside option (\code{j = 0}) sits in a singleton
nest with \code{lambda = 1}.
}
\note{
Unlike \code{\link[=simulate_mnl_data]{simulate_mnl_data()}} and \code{\link[=simulate_mxl_data]{simulate_mxl_data()}}, this
function does not expose \code{outside_option} or \code{vary_choice_set} flags.
The outside option (\code{j = 0}) is always present as a singleton nest with
\code{lambda = 1}, and every individual faces the full set of inside
alternatives. Add these flags if downstream use cases need them.
}
\examples{
\donttest{
sim <- simulate_nl_data(N = 2000, seed = 123)
print(sim)
}
}
