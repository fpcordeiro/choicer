% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmnlogit_utils.R
\name{run_hmnlogit}
\alias{run_hmnlogit}
\title{Fit a hierarchical Bayesian multinomial logit (HMNL)}
\usage{
run_hmnlogit(
  data = NULL,
  id_col = NULL,
  alt_col = NULL,
  choice_col = NULL,
  covariate_cols = NULL,
  person_col = NULL,
  alt_covariate_cols = NULL,
  outside_opt_label = NULL,
  cf_residual_col = NULL,
  input_data = NULL,
  include_outside_option = TRUE,
  rc_dist = NULL,
  prior = list(),
  mcmc = list(),
  chains = 1,
  keep_beta_i = c("means", "draws", "none"),
  keep_data = TRUE
)
}
\arguments{
\item{data}{Data frame (convenience pathway). Supply either \code{data} (with
the column names) or \code{input_data}, not both.}

\item{id_col}{Name of the column identifying choice situations (tasks).
Task ids only need to be unique within a respondent.}

\item{alt_col}{Name of the column identifying alternatives.}

\item{choice_col}{Name of the column indicating the chosen alternative
(1 = chosen, 0 = not chosen).}

\item{covariate_cols}{Vector of names of structural covariate columns
(the random-coefficient dimensions).}

\item{person_col}{Name of the respondent column grouping choice
situations. \code{NULL} (default) makes each choice situation its own
respondent.}

\item{alt_covariate_cols}{Names of alternative-level covariate columns
(constant within each alternative) forming the \eqn{\delta} mean
function. \code{NULL} (default) gives an intercept-only design (P = 1).}

\item{outside_opt_label}{Label of physical outside-option rows, removed
when \code{include_outside_option = TRUE} (the outside good is implicit).}

\item{cf_residual_col}{Name of a first-stage residual column (control
function for an endogenous covariate), appended to \code{X}. Default \code{NULL}.}

\item{input_data}{A \code{choicer_data_hmnl} object from \code{\link[=prepare_hmnl_data]{prepare_hmnl_data()}}
(advanced pathway).}

\item{include_outside_option}{Logical; if \code{TRUE} (default) an implicit
outside option with systematic utility 0 is part of every choice set.}

\item{rc_dist}{Integer vector, one entry per column of \code{covariate_cols}:
\code{0} for a normal random coefficient, \code{1} for log-normal (the
coefficient enters utility as \code{exp(beta_ik)}; hierarchy normal on the
log scale). Default \code{NULL} is all-normal. Automatically aligned through
dropped columns; a \code{cf_residual_col} coordinate is always normal.}

\item{prior}{Named list overriding prior defaults: \code{b_bar} (0), \code{A}
(0.01 I), \code{nu} (K + 3), \code{V} (nu I), \code{theta_bar} (0), \code{A_theta}
(0.01 I), \code{sd_prior} (list(half_cauchy = TRUE, s_d = 1, c0 = 3,
d0 = 3)).}

\item{mcmc}{Named list overriding MCMC defaults: \code{R} (10000), \code{burn}
(R \%/\% 5), \code{thin} (1), \code{seed} (drawn via \code{sample.int()} so
\code{set.seed()} governs), \code{trace} (0), \code{s_init} (2.38 / sqrt(K)),
\code{accept_target} (0.234).}

\item{chains}{Number of independent chains (seeds offset by 1). Chain 1
provides the reported draws; all chains feed the split-R-hat table.}

\item{keep_beta_i}{\code{"means"} (default) stores posterior means/SDs of the
individual-level \eqn{\beta_i}; \code{"draws"} additionally stores the full
(K, N, R_keep) draw cube (memory-guarded); \code{"none"} stores neither.}

\item{keep_data}{Logical; keep the prepared data on the fit (default
\code{TRUE}, needed by post-estimation methods).}
}
\value{
A \code{choicer_hmnl} object (classed \code{c("choicer_hmnl", "choicer_hb")}) with posterior summaries (\code{coefficients}, \code{se},
\code{vcov} for \eqn{b}; \code{theta_summary}; \code{sigma_d2_summary}; \code{W_mean};
\code{delta} and \code{xi} quality-ladder tables; \code{beta_i}), the raw thinned
\code{draws}, acceptance diagnostics in \code{accept}, the split-R-hat table in
\code{rhat} (when \code{chains > 1}), and sampler metadata.
}
\description{
Runs the adaptive RW-Metropolis-within-Gibbs sampler for the hierarchical
(random-coefficients, panel or cross-sectional) multinomial logit with a
BLP-style alternative-level random effect:
\deqn{U_{ijt} = x_{ijt}'\gamma_i + \delta_j + \epsilon_{ijt}, \qquad
      U_{iot} = \epsilon_{iot},}
with i.i.d. Gumbel shocks (including on the implicit outside option, whose
systematic utility is 0), \eqn{\beta_i \sim N(b, W)} over the structural
covariates (\eqn{\gamma_{ik} = \beta_{ik}} or \eqn{\exp(\beta_{ik})} per
\code{rc_dist}), and \eqn{\delta_j = z_j'\theta + \xi_j},
\eqn{\xi_j \sim N(0, \sigma_d^2)}. Partial pooling shrinks each
\eqn{\delta_j} toward its characteristics-based mean \eqn{z_j'\theta};
the outside option anchors the level of \eqn{\delta} (mean utility
relative to the outside good), so no base alternative or sum-to-zero
constraint is needed.
}
\details{
\strong{Initialization.} \eqn{\beta_i} start at the pooled MNL maximum
likelihood estimate over the structural covariates (log-normal
coordinates transformed to the chain scale with a warn-and-clamp at
0.05); \eqn{\delta} starts at shrunk log choice-share contrasts against
the outside option; \eqn{\theta} at the OLS regression of the initial
\eqn{\delta} on \code{Z}.

\strong{Priors.} \eqn{b \sim N(b\_bar, A^{-1})}, \eqn{W \sim IW(\nu, V)},
\eqn{\theta \sim N(\theta\_bar, A_\theta^{-1})}, and
\eqn{\sigma_d \sim} half-Cauchy\eqn{(0, s_d)} via the Makalic-Schmidt
scale mixture (set \code{sd_prior$half_cauchy = FALSE} for a plain
\eqn{IG(c_0, d_0)} on \eqn{\sigma_d^2}).

\strong{Endogeneity.} If a price-like covariate is endogenous (correlated
with \eqn{\xi_j}), supply a first-stage residual via \code{cf_residual_col}
(Petrin & Train 2010); posterior uncertainty does NOT propagate
first-stage estimation error.
}
\examples{
\donttest{
sim <- simulate_hmnl_data(N = 100, T = 3, J = 4, seed = 42)
fit <- run_hmnlogit(sim$data, "task", "alt", "choice", c("x1", "x2"),
                    person_col = "pid", alt_covariate_cols = "z1",
                    mcmc = list(R = 500, burn = 200))
summary(fit)
coef(fit, component = "delta")
}
}
\seealso{
\code{\link[=prepare_hmnl_data]{prepare_hmnl_data()}}, \code{\link[=simulate_hmnl_data]{simulate_hmnl_data()}},
\code{\link[=recovery_table]{recovery_table()}}, \code{\link[=rhat]{rhat()}}
}
