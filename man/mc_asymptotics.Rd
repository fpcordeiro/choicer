% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery.R
\name{mc_asymptotics}
\alias{mc_asymptotics}
\title{Asymptotic diagnostics for a Monte Carlo study}
\usage{
mc_asymptotics(
  mc,
  level = 0.95,
  se_col = "se",
  conv_threshold = 0.99,
  se_ratio_threshold_floor = 0.1
)
}
\arguments{
\item{mc}{A \code{choicer_mc} object returned by \code{\link[=monte_carlo]{monte_carlo()}}.}

\item{level}{Confidence level for the Wilson bands on coverage rates.
Defaults to \code{0.95}.}

\item{se_col}{Name of the column in \code{mc$replications} to use as the
standard-error source. Defaults to \code{"se"} (the Hessian-based SE stored
by \code{\link[=monte_carlo]{monte_carlo()}}). Callers that augment replications with an
alternative SE flavor (e.g., \code{"se_bhhh"} for a BHHH/OPG comparison)
can pass that column name to recompute every SE-dependent diagnostic
(\code{mean_se}, \code{se_ratio}, \code{mean_se_w}, \code{cov90/95/99}, z-moments,
normality tests, pass flags) against that flavor. Useful for the
information-matrix-equality check in Claim 4 of the MXL validation
suite.}

\item{conv_threshold}{Numeric in \verb{[0, 1]}. Minimum fraction of
replications that must converge for the per-parameter
\code{pass_convergence} flag to be \code{TRUE}. The flag compares
\code{R_used / R_total} (per parameter) against this threshold. Defaults
to \code{0.99}.}

\item{se_ratio_threshold_floor}{Numeric scalar. Minimum half-width for
the \code{pass_se_ratio} band. The actual band used is
\code{max(se_ratio_threshold_floor, 3 * 1.4 / sqrt(R_used))}, where the
\code{1.4 / sqrt(R)} term approximates the large-sample SD of
\code{mean_se / sd_emp}. The floor guarantees the band is never tighter
than the historical hard cutoff. Defaults to \code{0.10}.}
}
\value{
An object of class \code{choicer_mc_asymptotics} — a \code{data.table}
with one row per unique parameter and columns documented above — with
\code{meta} attached as an attribute (\code{attr(x, "meta")}).
}
\description{
Consumes a \code{choicer_mc} object and returns per-parameter asymptotic
diagnostics: Monte Carlo bias (with MC standard error), empirical SD of
the estimates, mean of the reported standard errors, SE-to-SD ratio
(information-matrix-equality check), Wald coverage at nominal 90 / 95 /
99 percent with Wilson confidence bands, moments of the studentized
statistic \code{z = (theta_hat - theta_0) / se}, and four normality tests on
\code{z} (Shapiro-Wilk, Anderson-Darling via \code{goftest::ad.test}, a hand-coded
Jarque-Bera statistic, and a one-sample Kolmogorov-Smirnov test against
\code{N(0, 1)}).
}
\details{
Six logical pass / fail flags are attached to every parameter row:
\code{pass_bias} requires \verb{|bias_mc_se| < 3}; \code{pass_se_ratio} requires
\verb{|se_ratio - 1|} to lie within \code{max(se_ratio_threshold_floor, 3 * 1.4 / sqrt(R_used))} (a noise-aware band that widens at small
\code{R_used} and tightens to the floor at large \code{R_used}); \code{pass_cov95}
requires the nominal 95 percent level to lie in the Wilson band for
empirical coverage; \code{pass_skew} requires \verb{|skew_z| < 0.3}; \code{pass_kurt}
requires excess kurtosis of \code{z} in \verb{[-0.5, 1.0]}; \code{pass_convergence}
requires the per-parameter convergence rate (\code{R_used / R_total}) to
meet \code{conv_threshold}.

Non-converged replications are excluded per parameter (reported in
\code{R_excluded}). Winsorized (5 percent / 95 percent) versions of \code{bias},
\code{sd_emp}, and \code{mean_se} are reported in parallel columns
(\code{bias_w}, \code{sd_emp_w}, \code{mean_se_w}) so silent outlier exclusion is
transparent to the reader. Two robust SE-to-SD ratios accompany the
Hessian-mean-based \code{se_ratio}: \code{se_ratio_med} (median SE divided by
the empirical SD) and \code{se_ratio_w} (winsorized mean SE divided by the
winsorized empirical SD); both stay near \code{1} when 1-2 replications
produce near-singular Hessians that inflate \code{mean_se}. The companion
\code{se_med} column reports the median per-replication SE used by
\code{se_ratio_med}. Neither robust ratio drives a \verb{pass_*} flag — they
are purely informational.

Winsorized z-moment counterparts (\code{mean_z_w}, \code{sd_z_w}, \code{skew_z_w},
\code{kurt_excess_z_w}) are reported alongside the raw z-moments and feed an
additional \code{pass_z_w} flag (Winsorized skew within the same band as
\code{pass_skew} AND Winsorized excess kurtosis within the same band as
\code{pass_kurt}). A companion \code{pass_cov95_w} flag is \code{TRUE} when either
\code{pass_cov95} is \code{TRUE} OR the per-rep Winsorized z-CI (the empirical
2.5 / 97.5 percentiles of the Winsorized z) covers truth-zero. These
two flags are designed for boundary scenarios (e.g., near-zero variance
components) where a small number of reps with vanishing SE inflate the
raw z-moments without indicating an estimator defect.
}
\examples{
\donttest{
sim_fun <- function(seed) simulate_mnl_data(N = 1000, J = 3, seed = seed)
fit_fun <- function(sim) run_mnlogit(
  data = sim$data, id_col = "id", alt_col = "alt", choice_col = "choice",
  covariate_cols = c("x1", "x2"), outside_opt_label = 0L,
  include_outside_option = FALSE, use_asc = TRUE,
  control = list(print_level = 0L)
)
mc <- monte_carlo(sim_fun, fit_fun, R = 50L, seed = 1L, progress = FALSE)
mc_asymptotics(mc)
}
}
