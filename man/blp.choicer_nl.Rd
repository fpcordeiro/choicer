% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{blp.choicer_nl}
\alias{blp.choicer_nl}
\title{BLP contraction mapping for nested logit model}
\usage{
\method{blp}{choicer_nl}(
  object,
  target_shares,
  delta_init = NULL,
  damping = 1,
  tol = 1e-08,
  max_iter = 1000,
  ...
)
}
\arguments{
\item{object}{A \code{choicer_nl} object fitted with \code{keep_data = TRUE}.}

\item{target_shares}{Numeric vector of target market shares.
Length \code{J_inside} when no outside option, or \code{J_inside + 1}
(with the outside option's share at index 1) when
\code{include_outside_option = TRUE}.}

\item{delta_init}{Initial guess for delta (ASC) values. If \code{NULL},
uses the estimated ASCs from the fitted model.}

\item{damping}{Contraction damping factor in (0, 1] (default 1).}

\item{tol}{Convergence tolerance (default 1e-8).}

\item{max_iter}{Maximum iterations (default 1000).}

\item{...}{Additional arguments (ignored).}
}
\value{
Converged delta (ASC) vector.
}
\description{
BLP contraction mapping for nested logit model
}
\examples{
\donttest{
library(data.table)
set.seed(42)
N <- 50; J <- 4
dt <- data.table(id = rep(1:N, each = J), alt = rep(1:J, N))
dt[, nest := rep(c(1L, 1L, 2L, 2L), N)]
dt[, `:=`(x1 = rnorm(.N), x2 = rnorm(.N))]
dt[, choice := 0L]
dt[, choice := sample(c(1L, rep(0L, J - 1))), by = id]
fit <- run_nestlogit(dt, "id", "alt", "choice", c("x1", "x2"), "nest")
blp(fit, target_shares = rep(1/J, J))
}
}
