% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{coef.choicer_mnp}
\alias{coef.choicer_mnp}
\title{Extract coefficients from a choicer_mnp object}
\usage{
\method{coef}{choicer_mnp}(object, ...)
}
\arguments{
\item{object}{A choicer_mnp object.}

\item{...}{Additional arguments (ignored).}
}
\value{
Named numeric vector of posterior means.
}
\description{
Returns the posterior means of the identified coefficients
(\eqn{\beta / \sqrt{\sigma_{11}}}, computed per draw).
}
\examples{
\donttest{
library(data.table)
set.seed(42)
N <- 100; J <- 3
dt <- data.table(id = rep(1:N, each = J), alt = rep(1:J, N))
dt[, `:=`(x1 = rnorm(.N), x2 = rnorm(.N))]
dt[, choice := 0L]
dt[, choice := sample(c(1L, rep(0L, J - 1))), by = id]
fit <- run_mnprobit(dt, "id", "alt", "choice", c("x1", "x2"),
                    mcmc = list(R = 300, burn = 100))
coef(fit)
}
}
