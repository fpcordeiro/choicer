% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{print.summary.choicer_nl}
\alias{print.summary.choicer_nl}
\title{Print summary for nested logit model}
\usage{
\method{print}{summary.choicer_nl}(x, ...)
}
\arguments{
\item{x}{A summary.choicer_nl object.}

\item{...}{Additional arguments (ignored).}
}
\value{
The object invisibly.
}
\description{
Print summary for nested logit model
}
\examples{
\donttest{
library(data.table)
set.seed(42)
N <- 50; J <- 4
dt <- data.table(id = rep(1:N, each = J), alt = rep(1:J, N))
dt[, `:=`(x1 = rnorm(.N), x2 = rnorm(.N))]
dt[, nest := ifelse(alt <= 2, "A", "B")]
dt[, choice := 0L]
dt[, choice := sample(c(1L, rep(0L, J - 1))), by = id]
fit <- run_nestlogit(
  data = dt, id_col = "id", alt_col = "alt", choice_col = "choice",
  covariate_cols = c("x1", "x2"), nest_col = "nest"
)
print(summary(fit))
}
}
