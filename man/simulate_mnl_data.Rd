% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_mnl_data}
\alias{simulate_mnl_data}
\title{Simulate multinomial logit data}
\usage{
simulate_mnl_data(
  N = 5000,
  J = 5,
  beta = c(0.8, -0.6),
  delta = NULL,
  seed = 123,
  outside_option = TRUE,
  vary_choice_set = TRUE
)
}
\arguments{
\item{N}{Number of choice situations.}

\item{J}{Number of inside alternatives.}

\item{beta}{Fixed coefficients for \verb{x1..x\{K_x\}} (length \code{K_x = length(beta)}).}

\item{delta}{Alternative-specific constants for inside alternatives
(length \code{J}). Defaults to an alternating pattern of \code{c(0.5, -0.5)}.}

\item{seed}{Random seed. Pass \code{NULL} to skip \code{set.seed()} (useful inside
\code{monte_carlo()} where the caller manages RNG).}

\item{outside_option}{Logical; if \code{TRUE} (default) an outside option with
\code{alt = 0} and zero covariates is added to every choice set.}

\item{vary_choice_set}{Logical; if \code{TRUE} (default) choice set size is
sampled uniformly from \code{2:J}; if \code{FALSE} every individual faces all \code{J}
inside alternatives.}
}
\value{
A \code{choicer_sim} object.
}
\description{
Generates synthetic choice data with i.i.d. Gumbel errors, optionally with
varying choice-set sizes and an outside option (alt = 0). Choices are
determined by argmax of utility; covariates are drawn as Uniform(-1, 1).
}
\examples{
\donttest{
sim <- simulate_mnl_data(N = 1000, J = 5, seed = 123)
print(sim)
}
}
