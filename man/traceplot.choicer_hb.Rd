% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hb_diagnostics.R
\name{traceplot.choicer_hb}
\alias{traceplot.choicer_hb}
\title{Traceplot method for hierarchical Bayes fits}
\usage{
\method{traceplot}{choicer_hb}(object, block = c("b", "theta", "sigma_d2"), which = NULL, ...)
}
\arguments{
\item{object}{A \code{choicer_hmnl} or \code{choicer_hmnp} fit.}

\item{block}{Character vector, any non-empty subset of \code{c("b", "theta", "sigma_d2", "delta")} (HMNP fits additionally accept \code{"sigma2"}, the raw
non-identified shock-variance chain). Default plots \code{b}, \code{theta},
\code{sigma_d2} (not \code{delta}, which can have ~200 columns).}

\item{which}{Only consulted when \code{"delta"} is in \code{block}. \code{NULL}
(default) auto-selects a representative subset (the 3 highest rank-R-hat
plus 3 lowest ESS_bulk alternatives, deduplicated, capped at 6). If
supplied, a character vector of alternative labels or an integer vector
of column indices.}

\item{...}{Additional arguments (ignored).}
}
\value{
\code{object}, invisibly.
}
\description{
Overlaid per-chain traceplots (base \code{graphics}, no new dependency) for a
\code{choicer_hmnl} / \code{choicer_hmnp} fit's population coefficients (\code{b}), delta
mean-function coefficients (\code{theta}), alternative-effect variance
(\code{sigma_d2}), and, opt-in, a representative subset of the (potentially
~200-column) alternative effects (\code{delta}).
}
\examples{
\donttest{
sim <- simulate_hmnl_data(N = 60, T = 2, J = 4, seed = 1)
fit <- suppressWarnings(run_hmnlogit(sim$data, "task", "alt", "choice", c("x1", "x2"),
                    person_col = "pid",
                    mcmc = list(R = 300, burn = 100), chains = 2))
traceplot(fit)
}
}
