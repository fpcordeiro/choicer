% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{diversion_ratios.choicer_nl}
\alias{diversion_ratios.choicer_nl}
\title{Diversion ratios for nested logit model}
\usage{
\method{diversion_ratios}{choicer_nl}(object, ...)
}
\arguments{
\item{object}{A \code{choicer_nl} object fitted with \code{keep_data = TRUE}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A J x J diversion ratio matrix with alternative labels.
}
\description{
Diversion ratios for nested logit model
}
\examples{
\donttest{
library(data.table)
set.seed(42)
N <- 50; J <- 4
dt <- data.table(id = rep(1:N, each = J), alt = rep(1:J, N))
dt[, nest := rep(c(1L, 1L, 2L, 2L), N)]
dt[, `:=`(x1 = rnorm(.N), x2 = rnorm(.N))]
dt[, choice := 0L]
dt[, choice := sample(c(1L, rep(0L, J - 1))), by = id]
fit <- run_nestlogit(dt, "id", "alt", "choice", c("x1", "x2"), "nest")
diversion_ratios(fit)
}
}
