% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery.R
\name{recovery_table}
\alias{recovery_table}
\alias{recovery_table.choicer_fit}
\alias{recovery_table.choicer_mc}
\title{Parameter recovery table}
\usage{
recovery_table(object, truth = NULL, level = 0.95, ...)

\method{recovery_table}{choicer_fit}(object, truth = NULL, level = 0.95, ...)

\method{recovery_table}{choicer_mc}(object, truth = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{choicer_fit} object (MNL, MXL, or NL) or a \code{choicer_mc}
result.}

\item{truth}{Either a \code{choicer_sim} object (whose \verb{$true_params} will be
used) or a named list of true parameter values.}

\item{level}{Confidence level for the Wald CI and coverage indicator.
Default \code{0.95}.}

\item{...}{Unused.}
}
\value{
See class-specific methods.
}
\description{
Compares fitted coefficients to a set of true parameter values on the same
scale as the estimator's internal parameterization. Returns one row per
estimated parameter with true value, estimate, standard error, bias,
relative bias (\%), z-score against the truth, Wald CI, and a coverage
indicator.
}
\details{
For MXL fits the \code{sigma} block compares the raw Cholesky parameters
(\code{L_params}), not the reconstructed covariance matrix. For log-normal
random-coefficient means the raw \code{mu} estimate is compared directly; callers
who want recovery on the DGP scale (\code{exp(mu)}) should transform both sides
before calling.

When the estimator has normalized the first inside alternative's ASC to
zero (which happens for MNL/MXL with \code{include_outside_option = FALSE} and
no outside option baked into the fit), the first entry of \code{truth$delta} is
dropped before the comparison so lengths match.
}
\section{Methods (by class)}{
\itemize{
\item \code{recovery_table(choicer_fit)}: Returns a \code{choicer_recovery} object (a
\code{data.table}) with columns \code{parameter}, \code{group}, \code{true}, \code{estimate},
\code{se}, \code{bias}, \code{rel_bias_pct}, \code{z_vs_true}, \code{lower_ci}, \code{upper_ci},
\code{covers}.

\item \code{recovery_table(choicer_mc)}: For a \code{choicer_mc} object, delegates to
\code{summary(object, level)} and returns a \code{choicer_mc_summary}. Inspect
\code{object$replications} directly for per-rep detail.

}}
\examples{
\donttest{
sim <- simulate_mnl_data(N = 2000, J = 4, seed = 123)
fit <- run_mnlogit(
  data = sim$data, id_col = "id", alt_col = "alt", choice_col = "choice",
  covariate_cols = c("x1", "x2"),
  outside_opt_label = 0L, include_outside_option = FALSE, use_asc = TRUE
)
recovery_table(fit, sim)
}
}
