% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hb_postest.R
\name{predict.choicer_hb}
\alias{predict.choicer_hb}
\title{Posterior choice probabilities and shares for hierarchical Bayes fits}
\usage{
\method{predict}{choicer_hb}(
  object,
  newdata = NULL,
  level = c("population", "individual"),
  n_draws = 200L,
  aggregate = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{choicer_hmnl} or \code{choicer_hmnp} fit.}

\item{newdata}{Data frame with the estimation columns (choice column not
required). \code{NULL} (default) predicts on the estimation data.}

\item{level}{\code{"population"} (default) integrates over \eqn{N(b, W)};
\code{"individual"} uses the respondent-level \eqn{\beta_i} (requires the
prediction rows to belong to estimation respondents; fully
posterior-integrated when the fit kept \code{keep_beta_i = "draws"}).}

\item{n_draws}{Number of posterior draws to integrate over (thinned
evenly from the kept draws; default 200).}

\item{aggregate}{If \code{TRUE} (default) return a per-alternative posterior
share table (including the outside option); if \code{FALSE} return the
posterior-mean probability per row of the prediction data.}

\item{...}{Ignored.}
}
\value{
With \code{aggregate = TRUE}, a \code{data.table} with columns
\code{alternative}, \code{share} (posterior mean), \code{sd}, \code{lower}, \code{upper} (95\%
equal-tailed interval); the posterior share draws are attached as
\code{attr(, "draws")}. With \code{aggregate = FALSE}, a numeric vector of
posterior-mean choice probabilities, one per prediction row.
}
\description{
Computes counterfactual choice probabilities, integrating over the
posterior draws and (at the population level) over the random-coefficient
distribution: for each kept draw \eqn{(b_r, W_r, \delta_r, \ldots)} one
\eqn{\beta \sim N(b_r, W_r)} is drawn and the model probabilities are
averaged. Alternatives in \code{newdata} that were not in the estimation
sample receive a posterior-predictive
\eqn{\delta_{new} \sim N(z_{new}'\theta_r, \sigma_{d,r}^2)} — the entry
counterfactual unlocked by the random-effects \eqn{\delta}. Price or
subsidy counterfactuals are just modified covariate columns in \code{newdata}.
}
\details{
HMNL probabilities are closed-form logit; HMNP probabilities use the
1-D Gauss-Hermite representation of the iid-probit integral
\eqn{P(j) = \int \phi(u) \prod_{k \ne j} \Phi(V_j - V_k + u) du}.
}
\examples{
\donttest{
sim <- simulate_hmnl_data(N = 100, T = 3, J = 4, seed = 42)
fit <- suppressWarnings(run_hmnlogit(sim$data, "task", "alt", "choice", c("x1", "x2"),
                    person_col = "pid", alt_covariate_cols = "z1",
                    mcmc = list(R = 500, burn = 200)))
predict(fit)                       # posterior shares, estimation data
cf <- sim$data
cf$x1 <- cf$x1 + 0.5               # a counterfactual attribute change
predict(fit, newdata = cf)
}
}
