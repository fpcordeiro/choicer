% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hb_diagnostics.R
\name{mcse}
\alias{mcse}
\title{Monte Carlo standard error of posterior summaries}
\usage{
mcse(draws, kind = c("mean", "median"))
}
\arguments{
\item{draws}{A matrix of posterior draws (rows = iterations, columns =
parameters) for a single chain, or a list of such matrices (one per
chain, identical dimensions).}

\item{kind}{\code{"mean"} (default) or \code{"median"}.}
}
\value{
Named numeric vector, one value per parameter (\code{NA} when the
underlying ESS or pooled SD is undefined).
}
\description{
Monte Carlo standard error (MCSE) of the posterior mean or median, using
the rank-normalized bulk/tail ESS from \code{\link{ess}}.
\code{kind = "mean"}: \code{MCSE = SD / sqrt(ess_bulk)}. \code{kind =
"median"}: \code{MCSE = sqrt(pi/2) * SD / sqrt(ess_tail)} (the closed-form
normal asymptotic-efficiency approximation; \code{2/pi} is the asymptotic
relative efficiency of the sample median vs. the mean under normality).
}
\examples{
set.seed(42)
draws <- matrix(rnorm(2000), ncol = 2,
                dimnames = list(NULL, c("a", "b")))
mcse(draws)
mcse(draws, kind = "median")
}
