% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_hmnl_data}
\alias{simulate_hmnl_data}
\title{Simulate hierarchical multinomial logit data}
\usage{
simulate_hmnl_data(
  N = 500,
  T = 10,
  J = 4,
  beta = c(0.8, -0.6),
  W = NULL,
  theta = c(0.5, -0.4),
  sigma_d = 0.5,
  Z = NULL,
  rc_dist = NULL,
  include_outside = TRUE,
  seed = 123,
  vary_choice_set = FALSE
)
}
\arguments{
\item{N}{Number of respondents.}

\item{T}{Number of choice situations per respondent.}

\item{J}{Number of inside alternatives.}

\item{beta}{Population means of the structural random coefficients
(length \code{K_x = length(beta)}; chain scale for log-normal coordinates).}

\item{W}{Covariance of the random coefficients (\code{K_x} x \code{K_x}).
Defaults to \code{diag(0.5, K_x)}.}

\item{theta}{Mean-function coefficients for
\eqn{\delta_j = z_j'\theta + \xi_j}; the first entry is the intercept,
entries \verb{2..P} load on the alternative-level covariates.}

\item{sigma_d}{Standard deviation of the alternative-level effects
\eqn{\xi_j}.}

\item{Z}{Optional \verb{J x (length(theta) - 1)} matrix of alternative-level
covariates (excluding the intercept). Default \code{NULL} draws them
Uniform(-1, 1).}

\item{rc_dist}{Integer vector (length \code{K_x}): \code{0L} for normal, \code{1L} for
log-normal coordinates. Default \code{NULL} is all-normal.}

\item{include_outside}{Logical; if \code{TRUE} (default) an outside option
with \code{alt = 0}, zero covariates, and its own Gumbel shock is added to
every choice set.}

\item{seed}{Random seed (\code{NULL} skips \code{set.seed()}).}

\item{vary_choice_set}{Logical; if \code{TRUE} choice-set size is sampled
uniformly from \code{2:J} per task. Default \code{FALSE}.}
}
\value{
A \code{choicer_sim} object. \code{true_params} contains \code{beta}, \code{W},
\code{theta}, \code{sigma_d}, the realized \code{delta} and \code{xi} vectors, the full
mean-function design \code{Z} (intercept first), and \code{rc_dist}.
}
\description{
Generates synthetic panel choice data from the hierarchical (random
coefficients + alternative-level random effects) logit DGP: respondents
\verb{i = 1..N} face \code{T} choice situations each, with utilities
\deqn{U_{ijt} = x_{ijt}'\gamma_i + \delta_j + \epsilon_{ijt}, \qquad
      U_{iot} = \epsilon_{iot},}
i.i.d. Gumbel shocks (including a shock on the outside option, whose
systematic utility is 0), \eqn{\beta_i \sim N(\beta, W)} with
\eqn{\gamma_{ik} = \beta_{ik}} or \eqn{\exp(\beta_{ik})} per \code{rc_dist},
and \eqn{\delta_j = z_j'\theta + \xi_j} with
\eqn{\xi_j \sim N(0, \sigma_d^2)}. Covariates are Uniform(-1, 1); the
alternative-level covariates \verb{z*} are constant within each alternative.
}
\details{
Log-normal coordinates are reported on the \emph{chain} (log) scale in
\code{true_params$beta} — the scale on which the estimator's hierarchy
operates — while entering utility as \code{exp(beta_ik)}.
}
\examples{
\donttest{
sim <- simulate_hmnl_data(N = 100, T = 4, J = 4, seed = 123)
print(sim)
sim$true_params$delta
}
}
