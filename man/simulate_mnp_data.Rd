% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_mnp_data}
\alias{simulate_mnp_data}
\title{Simulate multinomial probit data}
\usage{
simulate_mnp_data(
  N = 5000,
  J = 3,
  beta = c(0.8, -0.6),
  delta = NULL,
  Sigma = matrix(c(1, 0.5, 0.5, 1.5), nrow = 2),
  seed = 123
)
}
\arguments{
\item{N}{Number of choice situations.}

\item{J}{Number of alternatives (alternative 1 is the base).}

\item{beta}{Fixed coefficients for \verb{x1..x\{K_x\}} (length \code{K_x = length(beta)}).}

\item{delta}{ASCs of the differenced utilities, one per non-base
alternative (length \code{J - 1}). Defaults to an alternating pattern of
\code{c(0.5, -0.5)}.}

\item{Sigma}{Covariance matrix of the differenced errors
(\verb{(J-1) x (J-1)}).}

\item{seed}{Random seed (\code{NULL} skips \code{set.seed()}).}
}
\value{
A \code{choicer_sim} object. \code{true_params} contains \code{beta}, \code{delta},
and \code{Sigma} on the identified scale (see Details).
}
\description{
Generates synthetic choice data from the MNP data-generating process
estimated by \code{\link[=run_mnprobit]{run_mnprobit()}}: latent utility differences against the base
alternative (alternative 1),
\deqn{w_i = X_i \beta + \delta + \varepsilon_i, \qquad
      \varepsilon_i \sim N_{J-1}(0, \Sigma),}
with alternative \eqn{j > 1} chosen iff
\eqn{w_{ij} > \max(0, \max_{k \neq j} w_{ik})} and the base chosen iff all
\eqn{w_{ij} < 0}. Covariates are Uniform(-1, 1). Choice sets are balanced
(every individual faces all \code{J} alternatives), as the MNP estimator
requires; there is no outside-option flag — model an outside good as a
zero-covariate base alternative instead.
}
\details{
The MNP likelihood only identifies parameters up to scale, so
\code{true_params} is reported on the \emph{identified} scale (normalized by
\eqn{\sigma_{11}}): \code{beta} \eqn{= \beta / \sqrt{\sigma_{11}}}, \code{delta}
\eqn{= \delta / \sqrt{\sigma_{11}}}, and \code{Sigma} \eqn{= \Sigma /
\sigma_{11}} — the scale on which \code{\link[=run_mnprobit]{run_mnprobit()}} reports its posterior.
With the default \code{Sigma} (\eqn{\sigma_{11} = 1}) the DGP scale and the
identified scale coincide.
}
\examples{
\donttest{
sim <- simulate_mnp_data(N = 1000, J = 3, seed = 123)
print(sim)
}
}
