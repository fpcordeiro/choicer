% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery.R
\name{monte_carlo}
\alias{monte_carlo}
\title{Monte Carlo parameter recovery}
\usage{
monte_carlo(
  sim_fun,
  fit_fun,
  R = 100,
  seed = 1L,
  parallel = FALSE,
  progress = TRUE,
  ...
)
}
\arguments{
\item{sim_fun}{Function of \code{seed} returning a \code{choicer_sim}.}

\item{fit_fun}{Function of a \code{choicer_sim} returning a \code{choicer_fit}.}

\item{R}{Number of replications.}

\item{seed}{Base integer seed. Replication \code{r} uses \code{seed + r - 1L}.}

\item{parallel}{Logical; if \code{TRUE} and \code{future.apply} is available, run
replications in parallel using the user's active \code{future::plan()}.}

\item{progress}{Logical; print a one-line progress update per iteration in
serial mode. Ignored when \code{parallel = TRUE}.}

\item{...}{Unused.}
}
\value{
A \code{choicer_mc} object: a list with elements \code{replications} (a long
\code{data.table} with one row per estimated parameter per replication) and
\code{meta} (run metadata).
}
\description{
Replicates a \code{(DGP -> fit)} cycle \code{R} times with independent seeds and
collects per-parameter estimates, standard errors, bias, and coverage.
Returns a \code{choicer_mc} object; call \code{summary()} for aggregated statistics
(mean estimate, bias, RMSE, coverage rate, convergence rate).
}
\details{
Each iteration calls \code{sim_fun(seed = seed + r - 1L)}, then \code{fit_fun(sim)}.
Write \code{sim_fun} as a closure that captures \code{N}, \code{J}, and other DGP settings
and forwards \code{seed}. Write \code{fit_fun} as a closure that takes a
\code{choicer_sim} and returns a fitted \code{choicer_fit} object, wrapping any
data-preparation, draws, or optimizer-control setup.
}
\examples{
\donttest{
sim_fun <- function(seed) simulate_mnl_data(N = 1000, J = 4, seed = seed)
fit_fun <- function(sim) run_mnlogit(
  data = sim$data, id_col = "id", alt_col = "alt", choice_col = "choice",
  covariate_cols = c("x1", "x2"), outside_opt_label = 0L,
  include_outside_option = FALSE, use_asc = TRUE,
  control = list(print_level = 0L)
)
mc <- monte_carlo(sim_fun, fit_fun, R = 5, seed = 1L, progress = FALSE)
summary(mc)
}
}
