% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmnprobit_utils.R
\name{run_hmnprobit}
\alias{run_hmnprobit}
\title{Fit a hierarchical Bayesian multinomial probit (HMNP)}
\usage{
run_hmnprobit(
  data = NULL,
  id_col = NULL,
  alt_col = NULL,
  choice_col = NULL,
  covariate_cols = NULL,
  person_col = NULL,
  alt_covariate_cols = NULL,
  outside_opt_label = NULL,
  cf_residual_col = NULL,
  input_data = NULL,
  include_outside_option = TRUE,
  prior = list(),
  mcmc = list(),
  chains = 1,
  keep_beta_i = c("means", "draws", "none"),
  keep_data = TRUE
)
}
\arguments{
\item{data}{Data frame (convenience pathway). Supply either \code{data} (with
the column names) or \code{input_data}, not both.}

\item{id_col}{Name of the column identifying choice situations (tasks).
Task ids only need to be unique within a respondent.}

\item{alt_col}{Name of the column identifying alternatives.}

\item{choice_col}{Name of the column indicating the chosen alternative
(1 = chosen, 0 = not chosen).}

\item{covariate_cols}{Vector of names of structural covariate columns
(the random-coefficient dimensions).}

\item{person_col}{Name of the respondent column grouping choice
situations. \code{NULL} (default) makes each choice situation its own
respondent.}

\item{alt_covariate_cols}{Names of alternative-level covariate columns
(constant within each alternative) forming the \eqn{\delta} mean
function. \code{NULL} (default) gives an intercept-only design (P = 1).}

\item{outside_opt_label}{Label of physical outside-option rows, removed
when \code{include_outside_option = TRUE} (the outside good is implicit).}

\item{cf_residual_col}{Name of a first-stage residual column (control
function for an endogenous covariate), appended to \code{X}. Default \code{NULL}.}

\item{input_data}{A \code{choicer_data_hmnp} object from \code{\link[=prepare_hmnp_data]{prepare_hmnp_data()}}.}

\item{include_outside_option}{Logical; if \code{TRUE} (default) an implicit
outside option with systematic utility 0 is part of every choice set.}

\item{prior}{As in \code{\link[=run_hmnlogit]{run_hmnlogit()}}, plus \code{a0} (3) and \code{s0} (3), the
inverse-gamma shape/scale on the non-identified \eqn{\sigma^2}.}

\item{mcmc}{Named list overriding MCMC defaults: \code{R} (10000), \code{burn}
(R \%/\% 5), \code{thin} (1), \code{seed} (drawn via \code{sample.int()} so \code{set.seed()}
governs), \code{trace} (0). No proposal-scale settings: the sampler is fully
conjugate, with no Metropolis steps.}

\item{chains}{Number of independent chains (seeds offset by 1, run
sequentially). Chain 1 provides the reported draws; all chains feed the
rank-normalized split-R-hat table and the retained \code{chains} field (all
per-chain \code{b}/\code{w_vech}/\code{delta}/\code{theta}/\code{sigma_d2}/\code{loglik} draws,
consumed by \code{\link[=ess]{ess()}}, \code{\link[=mcse]{mcse()}}, and \code{\link[=traceplot]{traceplot()}}).}

\item{keep_beta_i}{\code{"means"} (default) stores posterior means/SDs of the
individual-level \eqn{\beta_i}; \code{"draws"} additionally stores the full
(K, N, R_keep) draw cube (memory-guarded, budgeted per chain); \code{"none"}
stores neither.}

\item{keep_data}{Logical; keep the prepared data on the fit (default
\code{TRUE}, needed by post-estimation methods).}
}
\value{
A \code{choicer_hmnp} object (classed \code{c("choicer_hmnp", "choicer_hb")}); the same layout as \code{\link[=run_hmnlogit]{run_hmnlogit()}}'s return, with all
reported summaries on the identified scale, raw chains in
\verb{draws$*_raw}, the non-identified \code{draws$sigma2} trace, and all chains'
retained draws (identified scale) in \code{chains}.
}
\description{
Runs the fully conjugate Albert-Chib Gibbs sampler for the hierarchical
multinomial probit with iid normal utility shocks in un-differenced
utility space:
\deqn{U_{ijt} = x_{ijt}'\beta_i + \delta_j + \epsilon_{ijt}, \qquad
      U_{iot} = \epsilon_{iot}, \qquad \epsilon \sim N(0, \sigma^2),}
choice by argmax within the task including the stochastic implicit
outside option, \eqn{\beta_i \sim N(b, W)} (normal coordinates only —
log-normal would break conjugacy), and
\eqn{\delta_j = z_j'\theta + \xi_j}, \eqn{\xi_j \sim N(0, \sigma_d^2)}.
}
\details{
\strong{Identification.} The probit likelihood is invariant to a common
rescaling of utilities and \eqn{\sigma}, so the chain runs on the
non-identified parameterization (free \eqn{\sigma^2}, better mixing via
parameter expansion) and every kept draw is normalized by the matching
power of the CURRENT \eqn{\sigma}: reported \eqn{b/\sigma},
\eqn{W/\sigma^2}, \eqn{\delta/\sigma}, \eqn{\theta/\sigma},
\eqn{\sigma_d^2/\sigma^2}. Raw chains are kept in \verb{draws$*_raw}. The
outside option anchors the location of \eqn{\delta} exactly as in
\code{\link[=run_hmnlogit]{run_hmnlogit()}}.

For \code{beta_i} draws at very large scale beyond the memory guard's
threshold, a future disk-streaming path (writing each kept slice to
disk instead of retaining it in memory) is on the roadmap but not built
in this phase; users needing per-respondent draws at that scale should
reduce \code{R}, reduce \code{chains}, or use \code{keep_beta_i = "means"}.
}
\examples{
\donttest{
sim <- simulate_hmnp_data(N = 100, T = 3, J = 4, seed = 42)
fit <- suppressWarnings(run_hmnprobit(sim$data, "task", "alt", "choice", c("x1", "x2"),
                     person_col = "pid", alt_covariate_cols = "z1",
                     mcmc = list(R = 500, burn = 200)))
summary(fit)
}
}
\seealso{
\code{\link[=prepare_hmnp_data]{prepare_hmnp_data()}}, \code{\link[=simulate_hmnp_data]{simulate_hmnp_data()}}, \code{\link[=run_hmnlogit]{run_hmnlogit()}},
\code{\link[=ess]{ess()}}, \code{\link[=mcse]{mcse()}}, \code{\link[=traceplot]{traceplot()}}
}
