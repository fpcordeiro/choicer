% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mxl_predict}
\alias{mxl_predict}
\title{Per-observation simulated choice probabilities for Mixed Logit}
\usage{
mxl_predict(
  theta,
  X,
  W,
  alt_idx,
  M,
  eta_draws,
  rc_dist,
  rc_correlation = TRUE,
  rc_mean = FALSE,
  use_asc = TRUE,
  include_outside_option = FALSE
)
}
\arguments{
\item{theta}{parameter vector (beta, [mu], L, delta)}

\item{X}{design matrix for fixed coefficients; sum(M_i) x K_x}

\item{W}{design matrix for random coefficients; sum(M_i) x K_w or J x K_w}

\item{alt_idx}{sum(M) x 1 vector with indices of alternatives; 1-based indexing}

\item{M}{N x 1 vector with number of alternatives for each individual}

\item{eta_draws}{Array with draws; K_w x S x N}

\item{rc_dist}{K_w vector indicating distribution (0=normal, 1=log-normal)}

\item{rc_correlation}{whether random coefficients are correlated}

\item{rc_mean}{whether mu parameters are estimated}

\item{use_asc}{whether ASCs are included}

\item{include_outside_option}{whether the outside option is present}
}
\value{
List with \code{choice_prob} (length sum(M)), \code{utility} (length sum(M),
simulated mean of the deterministic + W*gamma component), and, when
\code{include_outside_option = TRUE}, \code{choice_prob_outside} (length N).
}
\description{
Returns the simulated choice probability for each (individual, alternative)
row of \code{X}, averaged over the supplied Halton draws. Mirrors \code{mnl_predict}.
}
