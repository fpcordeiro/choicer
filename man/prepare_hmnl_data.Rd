% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmnlogit_utils.R
\name{prepare_hmnl_data}
\alias{prepare_hmnl_data}
\title{Prepare inputs for hierarchical multinomial logit estimation}
\usage{
prepare_hmnl_data(
  data,
  id_col,
  alt_col,
  choice_col,
  covariate_cols,
  person_col = NULL,
  alt_covariate_cols = NULL,
  outside_opt_label = NULL,
  cf_residual_col = NULL,
  include_outside_option = TRUE,
  rc_dist = NULL
)
}
\arguments{
\item{data}{Data frame containing choice data.}

\item{id_col}{Name of the column identifying choice situations (tasks).
Task ids only need to be unique within a respondent.}

\item{alt_col}{Name of the column identifying alternatives.}

\item{choice_col}{Name of the column indicating the chosen alternative
(1 = chosen, 0 = not chosen).}

\item{covariate_cols}{Vector of names of structural covariate columns
(the random-coefficient dimensions).}

\item{person_col}{Name of the respondent column grouping choice
situations. \code{NULL} (default) makes each choice situation its own
respondent.}

\item{alt_covariate_cols}{Names of alternative-level covariate columns
(constant within each alternative) forming the \eqn{\delta} mean
function. \code{NULL} (default) gives an intercept-only design (P = 1).}

\item{outside_opt_label}{Label of physical outside-option rows, removed
when \code{include_outside_option = TRUE} (the outside good is implicit).}

\item{cf_residual_col}{Name of a first-stage residual column (control
function for an endogenous covariate), appended to \code{X}. Default \code{NULL}.}

\item{include_outside_option}{Logical; if \code{TRUE} (default) an implicit
outside option with systematic utility 0 is part of every choice set.}

\item{rc_dist}{Integer vector, one entry per column of \code{covariate_cols}:
\code{0} for a normal random coefficient, \code{1} for log-normal (the
coefficient enters utility as \code{exp(beta_ik)}; hierarchy normal on the
log scale). Default \code{NULL} is all-normal. Automatically aligned through
dropped columns; a \code{cf_residual_col} coordinate is always normal.}
}
\value{
A list of class \code{c("choicer_data_hmnl", "list")} containing:
\itemize{
\item \code{X}: Structural design matrix (total_rows x K_struct), no ASC
columns; \code{cf_residual_col} last when supplied.
\item \code{alt_of_row}: Integer alternative code per row (\verb{1..J}).
\item \code{alt_idx}: Alias of \code{alt_of_row} for the pooled-MLE init.
\item \code{Z}: Alternative-level design (J x P), intercept first.
\item \code{M}: Inside alternatives per choice situation.
\item \code{choice_pos}: 1-based within-task position of the chosen row;
\code{0} = outside option chosen.
\item \code{Ti}: Choice situations per respondent.
\item \code{person_ids}, \code{N_persons}, \code{n_tasks}, \code{J}, \code{K_struct}, \code{P}.
\item \code{include_outside_option}: Logical flag.
\item \code{alt_mapping}: Data.table mapping alternatives to summary
statistics (outside option is \code{alt_int = 0}).
\item \code{param_map}: Named list of index vectors (\code{beta}, \code{theta}),
robust to collinearity drops.
\item \code{rc_dist}: Integer vector aligned with the columns of \code{X}.
\item \code{dropped_cols}, \code{dropped_z_cols}: Dropped column names, if any.
\item \code{data_spec}: Column-name metadata (incl. \code{person_col},
\code{outside_opt_label}, \code{cf_residual_col}, \code{alt_covariate_cols}).
}
}
\description{
Prepares and validates panel (or cross-sectional) choice data for the
hierarchical Bayesian multinomial logit. The model has two random-effect
levels: respondent-level structural tastes \eqn{\beta_i \sim N(b, W)}
over the \code{covariate_cols}, and a global alternative-level effect
\eqn{\delta_j = z_j'\theta + \xi_j}, \eqn{\xi_j \sim N(0, \sigma_d^2)},
with mean-function design \eqn{z_j} built from \code{alt_covariate_cols}.
}
\details{
\strong{Structure.} The design matrix \code{X} carries structural covariates
only — no alternative-specific-constant dummies. The alternative effect
\eqn{\delta_j} is indexed by \code{alt_of_row} (integer codes \verb{1..J}), so
memory and compute scale with the number of rows, not with \code{J} extra
design columns.

\strong{Outside option.} With \code{include_outside_option = TRUE} (the
default) the outside good is modelled implicitly, following the
\code{\link[=prepare_mnl_data]{prepare_mnl_data()}} convention: physical outside rows (identified by
\code{outside_opt_label}) are removed, the estimation kernels add the outside
term (systematic utility 0), and a choice situation whose inside rows are
all \code{0} in \code{choice_col} is coded as "outside chosen" (\code{choice_pos = 0}).
The outside option anchors the location of \eqn{\delta} (mean utility
relative to the outside good).

\strong{Cross-section vs panel.} \code{person_col} groups choice situations
into respondents sharing one \eqn{\beta_i}. With \code{person_col = NULL}
(default) every choice situation is its own respondent (\code{Ti} all 1) —
the cross-sectional random-coefficients mode.

\strong{Control function.} \code{cf_residual_col} (a user-supplied first-stage
residual, Petrin & Train 2010) is appended to \code{X} as an ordinary
covariate; its provenance is recorded in \code{data_spec}. The first stage is
NOT run here — supplying a valid residual is the user's responsibility.
}
\examples{
library(data.table)
set.seed(42)
N <- 20; T <- 3; J <- 4
dt <- data.table(
  pid  = rep(1:N, each = T * J),
  task = rep(seq_len(N * T), each = J),
  alt  = rep(1:J, N * T)
)
dt[, `:=`(x1 = rnorm(.N), x2 = runif(.N, -1, 1))]
dt[, quality := 0.1 * alt]                # alternative-level covariate
dt[, choice := 0L]
# leave some tasks all-zero: outside option chosen
dt[, choice := if (runif(1) < 0.8) sample(c(1L, rep(0L, J - 1))) else 0L,
   by = task]
input <- prepare_hmnl_data(dt, "task", "alt", "choice", c("x1", "x2"),
                           person_col = "pid",
                           alt_covariate_cols = "quality")
str(input$Z)
input$alt_mapping
}
\seealso{
\code{\link[=prepare_hmnp_data]{prepare_hmnp_data()}} for the hierarchical probit counterpart.
}
