% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{blp}
\alias{blp}
\title{BLP contraction mapping}
\usage{
blp(object, target_shares, ...)
}
\arguments{
\item{object}{A fitted model object.}

\item{target_shares}{Numeric vector of target market shares (length J).}

\item{...}{Additional arguments passed to methods.}
}
\value{
Converged delta (ASC) vector.
}
\description{
Finds the ASC (delta) parameters such that predicted market shares match
target shares, using the contraction mapping of Berry, Levinsohn, and
Pakes (1995).
}
\examples{
\donttest{
library(data.table)
set.seed(42)
N <- 50; J <- 3
dt <- data.table(id = rep(1:N, each = J), alt = rep(1:J, N))
dt[, `:=`(x1 = rnorm(.N), x2 = rnorm(.N))]
dt[, choice := 0L]
dt[, choice := sample(c(1L, rep(0L, J - 1))), by = id]
fit <- run_mnlogit(dt, "id", "alt", "choice", c("x1", "x2"))
blp(fit, target_shares = rep(1/J, J))
}
}
