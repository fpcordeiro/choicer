# Multinomial Probit Benchmarks

This folder currently holds the legacy Bayesian multinomial probit benchmark.
It has not yet been migrated to the structured benchmark infrastructure used by
`_benchmarks/mnlogit/`.
